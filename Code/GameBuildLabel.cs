namespace Game;

/// <summary>
/// Visible build label (bottom-right watermark + assistant chat footer bump this constant together).
/// Versioning: bump patch (third) each prompt. The minor (middle) segment moves only when the user asks for it — there is no automatic rollover.
/// </summary>
public static class GameBuildLabel
{
	public const string Display = "0.8.2253";
}
