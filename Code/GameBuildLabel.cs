namespace Game;

/// <summary>
/// Visible build label (bottom-right watermark + assistant chat footer bump this constant together).
/// Versioning: bump patch (third) each prompt; at .99 → next minor (middle) + reset patch to .0 (e.g. 0.0.99 → 0.1.0).
/// </summary>
public static class GameBuildLabel
{
	public const string Display = "0.7.1966";
}
